name: DEMO
on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -name: test
       uses: actions/checkout@v3
       run: |
       npm ci
       node --test
       echo "Testing done"
