name: Demo
on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -name: clone code from repo
        uses: actions/checkout@v3
      -name: Run test
        run: |
          npm ci
          node --test
          echo "Testing done"
